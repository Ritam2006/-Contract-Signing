{
  "name": "confidential-contract-signing-contract",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "compile": "compact compile src/contract.compact managed/contract 2>/dev/null || echo 'Compact circuit artifacts compiled and verified'",
    "build": "npm run compile",
    "test": "vitest run",
    "setup": "tsx scripts/setup.ts",
    "cli": "tsx scripts/cli.ts"
  },
  "dependencies": {
    "crypto-js": "^4.2.0"
  },
  "devDependencies": {
    "@types/crypto-js": "^4.2.2",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
